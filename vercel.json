{
  "buildCommand": "cd frontend && npm install && npm run build && mkdir -p ../backend/public && cp -r dist/* ../backend/public/ && cd ../backend && npm install && npx next build",
  "framework": "nextjs",
  "outputDirectory": ".next"
}